{
  "type": "object",
  "properties": {
    "price_range": {
      "type": "object",
      "properties": {
        "min": {
          "type": "number",
          "description": "Minimum price in the range."
        },
        "max": {
          "type": "number",
          "description": "Maximum price in the range."
        }
      },
      "required": [
        "min",
        "max"
      ]
    },
    "category": {
      "type": "string",
      "description": "Optional category to filter the menu items (e.g., 'Appetizers', 'Soup')."
    }
  },
  "required": [
    "price_range"
  ]
}